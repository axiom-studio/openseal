# Binaries
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary
*.test

# Output of the go coverage tool
*.out

# Dependency directories
vendor/

# Go workspace file
go.work

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Build output
bin/
dist/
web/node_modules/
web/dist/
desktop/node_modules/
desktop/dist/
desktop/src-tauri/target/
desktop/crates/*/target/

# Local config (root level only)
/.env
/.env.*
!/.env.example
/daemon.yaml
/context.yaml
/workflow.hcl
/data/*.db
/data/*.db-*
/skills/.clawhub/
/skills/repos/
# Anchored: this is the `make build` output at the repository root. Unanchored,
# it also matches the cmd/openseal/ source directory, which silently ignores
# every NEW file added there — existing files survive only because tracked
# files are exempt from ignore rules.
/openseal

# Desktop development and generated assets
desktop/.dev-workspace/
desktop/src-tauri/binaries/
desktop/src-tauri/THIRD_PARTY_NOTICES
desktop/src-tauri/gen/
desktop/test-results/
desktop/playwright-report/
desktop/*.tsbuildinfo
.impeccable/review/

# Python verification caches
__pycache__/
*.pyc
